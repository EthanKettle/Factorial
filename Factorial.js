function factorial (f,fullFactorial) {
    let i = 0
    while (i < f) {
        fullFactorial = fullFactorial * f
        f--;
        i++;
        return fullFactorial
        return f
    }
}

let factor = 7;
let num = 1;

console.log(factorial(factor,num))
